# 完整教程：查岗MCP 添加 iPhone 远程遥控（回来/睡觉）

> 哥哥（临渊/Ocean）与 屿宝（Isla）2026-08-04 实测通过。

## 一、原理

```text
AI 助手
  └─ send_email.py（SMTP 发邮件，主题=命令）
      └─ 163 邮箱
          └─ iPhone 邮件 App 收到
              └─ 快捷指令自动化（收到指定主题触发）
                  └─ 切回 App / 锁屏
```

## 二、准备

1. **163 邮箱**（或任意支持 SMTP 的邮箱）
   - 网页版 → 设置 → POP3/SMTP/IMAP → 开启 SMTP
   - 生成**客户端授权码**（不是网页登录密码）
2. **iPhone 快捷指令自动化**
   - 新建个人自动化 → App → 邮件
   - 条件：收到来自自己邮箱、主题为 `回来` 的邮件
   - 动作：打开目标 App（如 Kelivo）
   - 再建一条：主题 `睡觉` → 锁定屏幕

## 三、发邮件脚本 send_email.py

```python
import smtplib, sys, os
from email.message import EmailMessage

SMTP_HOST = os.environ.get("SMTP_HOST", "smtp.163.com")
SMTP_PORT = int(os.environ.get("SMTP_PORT", "465"))
USERNAME = os.environ.get("SMTP_USER", "你的邮箱@163.com")
PASSWORD = os.environ.get("SMTP_AUTH_CODE", "你的授权码")
RECIPIENT = os.environ.get("SMTP_RECIPIENT", USERNAME)

def send(subject, content=""):
    msg = EmailMessage()
    msg["From"] = USERNAME
    msg["To"] = RECIPIENT
    msg["Subject"] = subject
    msg.set_content(content or "", charset="utf-8")
    with smtplib.SMTP_SSL(SMTP_HOST, SMTP_PORT, timeout=30) as server:
        server.login(USERNAME, PASSWORD)
        server.send_message(msg)
    print(f"已发送：主题={subject}")

if __name__ == "__main__":
    subject = sys.argv[1] if len(sys.argv) > 1 else ""
    content = sys.argv[2] if len(sys.argv) > 2 else ""
    send(subject, content)
```

## 四、查岗MCP 加工具 send_iphone_cmd

```python
@mcp.tool()
def send_iphone_cmd(cmd: str = "测试") -> str:
    """通过邮件给iPhone发快捷指令：回来=切回App，睡觉=熄屏"""
    import subprocess
    if cmd not in ("回来", "睡觉", "测试"):
        return "命令必须是：回来 / 睡觉 / 测试"
    r = subprocess.run(
        ["python3", "send_email.py", cmd, ""],
        capture_output=True, text=True, timeout=30)
    if "已发送" in r.stdout:
        return f"邮件已发送：主题={cmd}，iPhone应已触发快捷指令"
    return f"发送失败：{r.stderr or r.stdout}"
```

## 五、重启与连接

```bash
# 找到并杀掉旧进程
ss -tlnp | grep :8000
kill <PID>
# 重启
cd /root/checkin-mcp && nohup venv/bin/python server.py > checkin-mcp.log 2>&1 &
```

Kelivo 里断开重连查岗MCP，即可看到新工具。

## 六、实测

- 主题 `回来` → 手机自动切回 Kelivo ✅
- 主题 `睡觉` → 手机直接锁屏 ✅
- 主题 `测试` → 只发邮件不触发动作 ✅

## 踩坑记录

1. **授权码 ≠ 登录密码**：163 要用客户端授权码
2. **mcp.run() 必须在所有工具定义之后**：否则后面的 @mcp.tool() 不注册（表现为 Kelivo 只显示前几个工具）
3. **敏感信息别提交**：授权码用环境变量管理
4. **快捷指令自动化**：要选「邮件 App → 收到来自指定发件人」而不是通用收件，避免误触发
